public class SingleArrays {
    public static void main(String[] args) {
        // Declare and initialize an integer array
        int[] ages = {25, 30, 22, 28, 26};
        
        // Declare and initialize a String array
        String[] names = {"Jack", "Mary", "Chisom", "Kim", "Becca"};
        
        // Creating a new array using the new keyword
        numbers[] = new int[5];  // Array of size 5
        numbers[0] = 15;
        numbers[1] = 30;
        numbers[2] = 22;  // Changed from numbers[3] to numbers[2] to avoid an index out of bounds error
        numbers[3] = 12;
        numbers[4] = 11;   // Corrected index, no out-of-bounds error
        
        // Create an 'ages' array for the missing 'ages[0]' reference
        
        
        // Print the arrays (example of usage)
        System.out.printf("Name is %s and age is %d%n", names[0], ages[0]); // Output: Name is Jack and age is 25
        System.out.printf("The number at the 2nd position is: %d%n", numbers[1]); // Output: The number at the 2nd position is: 30
    }
}
